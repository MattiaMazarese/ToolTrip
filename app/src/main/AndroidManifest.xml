<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToolTrip"
        tools:targetApi="31">
        <activity
            android:name="login.ProfileActivity"
            android:exported="false" />
        <activity
            android:name="login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="login.LoginActivity" />
        <activity android:name="login.RegisterActivity" />
        <activity android:name="menù.HomeActivity" />
        <activity android:name="item.AggiungiToolActivity" />
        <activity android:name="item.VisualizzaToolActivity" />
        <activity android:name="item.VisualizzaProdottoSingoloActivity" />
        <activity android:name="item.ToolPersonaliUtente" />
        <activity android:name="group.MyGroupActivity"/>
        <activity android:name="group.VisualizzaGruppoSingoloActivity" />
        <activity android:name="group.SearchGroupActivity" />
        <activity android:name="group.CreateGroupActivity" />
        <activity android:name="group.SelectGroupActivity" />
    </application>

</manifest>