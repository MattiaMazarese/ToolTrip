<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToolTrip"
        tools:targetApi="31">
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- LoginActivity -->
        <activity android:name=".LoginActivity" /> <!-- RegisterActivity -->
        <activity android:name=".RegisterActivity" /> <!-- HomeActivity -->
        <activity android:name=".HomeActivity" /> <!-- SelectGroupActivity -->
        <activity android:name=".SelectGroupActivity" /> <!-- AggiungiItemActivity -->
        <activity android:name=".AggiungiToolActivity" /> <!-- VisualizzaToolActivity -->
        <activity android:name=".VisualizzaToolActivity" />
        <activity android:name=".SearchGroupActivity" />
        <activity android:name=".CreateGroupActivity" />
        <activity android:name=".VisualizzaProdottoSingoloActivity" />
        <activity android:name=".ToolPersonaliUtente" />
    </application>

</manifest>